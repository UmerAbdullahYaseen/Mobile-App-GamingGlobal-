/* import React, { useEffect, useState} from "react";
import { View, Text, StyleSheet } from "react-native";
import tailwind from 'tailwind-rn';
import LinearGradient from 'react-native-linear-gradient';
const LinearImageSection = (props) => {
  const mediaMatch = window.matchMedia("(max-width: 600px)");
 /*  /* const [matches, setMatches] = useState(mediaMatch.matches); */

 /*  useEffect(() => {
    const handler = (e) => setMatches(e.matches);
    mediaMatch.addListener(handler);
    return () => mediaMatch.removeListener(handler);
  });

  const styles = (isMobile) => {
    return {
      backgroundImage: `url(${props.imageUrl})`,
      height: isMobile ? '30vh' : '60vh',
    }
  };
 */











 /*  const linearBackgroundStyle = {
    background:
      "linear-gradient(0deg, rgba(31, 29, 41, 1) 1%, rgba(255, 255, 255, 0) 49%, rgba(31, 29, 41, 1) 95%)",
      
  };

  return (
    <View
      className="text-green-50  bg-cover bg-center bg-no-repeat h-1/2 sm:bg-center sm:bg-cover "
    >
        <LinearGradient colors={['#4c669f', '#3b5998', '#192f6a']} style={styles.linearGradient}>
               <Text style={styles.buttonText}>
                Sign in with Facebook
               </Text>
        </LinearGradient>

        
      <View className="h-full w-full flex items-center justify-center lg:justify-start " style={linearBackgroundStyle}>
        <View className="lg:ml-52">{props.children}</View>
      </View>
    </View>
  );
}; */


 
/* const styles = StyleSheet.create({
  linearGradient: {
    flex: 1,
    paddingLeft: 15,
    paddingRight: 15,
    borderRadius: 5
  },
  buttonText: {
    fontSize: 18,
    fontFamily: 'Gill Sans',
    textAlign: 'center',
    margin: 10,
    color: '#ffffff',
    backgroundColor: 'transparent',
  },
});
export default LinearImageSection;
 */